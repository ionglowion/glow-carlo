{
  "name": "glow-carlo",
  "version": "0.9.47",
  "description": "Carlo is a framework for rendering Node data structures using Chrome browser.",
  "repository": "https://github.com/ionglowion/glow-carlo",
  "engines": {
    "node": ">=7.6.0"
  },
  "main": "index.js",
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "lint": "([ \"$CI\" = true ] && eslint --quiet -f codeframe . || eslint .)",
    "test": "node rpc/test.js && node test/test.js",
    "headful-test": "node test/headful.js"
  },
  "keywords": [],
  "author": "The Chromium Authors",
  "license": "Apache-2.0",
  "dependencies": {
    "debug": "^4.1.0",
    "puppeteer-core": "^1.20.0"
  },
  "devDependencies": {
    "@pptr/testrunner": "^0.8.0",
    "@pptr/testserver": "^0.5.0",
    "eslint": "^6.5.1"
  }
}
